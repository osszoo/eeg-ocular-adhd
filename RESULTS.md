# 결과 요약 (refinement 브랜치)

이 문서는 refinement 브랜치에서 수행한 세 단계(4단계 IC 제거 비용 arm →
2단계 에폭 중첩 → 3단계 상한 측정)의 결과를 정리한다. 사전등록은 `PREREG.md`,
원 수치는 각 로그 파일에 있으며, 이 문서는 그 요약이다. 수치는 재산출한 것이
아니라 커밋된 로그(`results/conditions/contrast_all.txt`,
`results/overlap/contrast_overlap.txt`, `results/ceiling/*.txt`)에서 옮긴 것이다.

주지표는 사람 단위 pooled OOF AUC이며, 정확도·민감도·특이도는 2차 지표다.
신뢰구간은 사람 단위 짝 부트스트랩(피험자 재표집)으로, 피험자 표집 변동만
반영하고 모델 학습 변동은 포함하지 않는다(PREREG 3.3).


## 1. 연구 질문과 가설

ICA로 분리되어 통상 폐기되는 안구 독립성분(eye IC)이 ADHD 판별 정보를 담고
있으며, 신경 특징에 추가하면 분류 성능이 향상된다.

데이터는 Nasrabadi et al.(IEEE DataPort, DOI 10.21227/rzfh-zn36), ADHD 61명 /
대조 60명, 19채널 128Hz. 전처리는 bandpass 0.5-40Hz(zero-phase FIR) -> CAR ->
ASR(cutoff=100) -> ICA(extended Infomax, random_state=97) -> ICLabel(tau=0.5,
argmax). 6초 비중첩 에폭. 분류는 StratifiedGroupKFold K=5(사람 단위), fold 내부
mRMR k=30, Logistic C=1.0, 에폭 확률을 사람별로 평균하여 pooled AUC 산출.

표본은 채널 품질 스크리닝(PREREG 8.1)으로 3층 정의: full121 / tier1_91 /
tier2_86(주). 안구 조건은 eye argmax IC(>=0.5)를 가진 피험자만 정의되어
roster84 / roster59로 별도 표본을 이룬다. 121명 중 84명(69%)이 자격 eye IC를
보유하며 군 편향은 없다(ADHD 72% / 대조 67%).

비교 대상 선행연구는 EEG-TACT(Pastrana-Cortes et al., Technologies 2026)로,
같은 데이터셋·프로토콜에서 사람 단위 정확도 87.5%를 보고했다. 차이는 ICA/ASR
없음, 2초 50% 중첩, EEGNet+Transformer, fold별 Optuna 20회, 0.5-60Hz다.


## 2. 4단계 — IC 제거 비용 arm

피험자당 ICA를 1회 수행하고 역투영으로 19개 조건 신호를 생성했다
(`src/conditions.py`). 역투영 경로는 항등 검사로 직접 검증했다
(`ica.apply(exclude=[])` 최대편차 2.7e-18 vs 신호규모 3.2e-3).

### 2.1 전처리 절제 사다리

각 전처리 단계를 하나씩 제거했을 때의 AUC 변화(양수 = 제거가 AUC를 올림 =
그 단계가 정보를 버리고 있었음):

| 조건 | 대비 | roster84 dAUC | 판정 |
|---|---|---|---|
| remove ICA | i_brain -> iii_noica | **+0.081 [+0.019, +0.147]** | CI>0 |
| remove ASR | | 전 표본 무효 | CI에 0 포함 |
| widen band 0.5-60 | | 전 표본 무효 | CI에 0 포함 |

remove ICA의 효과는 tier 사다리(full121 +0.015 / tier1_91 +0.009 /
tier2_86 +0.032)에서는 전부 CI가 0을 포함하여 서로 구별되지 않았고,
**roster84에서만 CI>0**였다. 즉 효과는 tier 정제 여부가 아니라 **안구 IC 보유
여부**에 붙어 있다. 이는 3단계에서도 재확인된다(§4.1).

### 2.2 안구 가설 가지

안구 성분을 신경 특징에 더했을 때의 AUC 증가:

| 표본 | 대비 | dAUC | 판정 |
|---|---|---|---|
| roster84 | ii_eye1 (주) | +0.043 [-0.006, +0.098] | **불확정** |
| roster84 | ii_eyeall (2차) | **+0.069 [+0.015, +0.131]** | CI>0 |
| roster59 | ii_eye1 (주) | +0.056 [-0.011, +0.129] | 미지지 |
| roster59 | ii_eyeall (2차) | **+0.081 [+0.012, +0.162]** | CI>0 |

주 대비 ii_eye1은 판정 규칙 (a)CI 하한>0을 충족하지 못했다. 다만 (b)13개 대조를
전부 상회하는 조건은 충족하여 **불확정**으로 남는다. 2차 대비 ii_eyeall은
roster84·roster59 양쪽에서 CI 하한>0이다.

대조군(PREREG 8.3.1): 원본 크기(ii_c), 분산 정합(ii_m), 위상 무작위 대리(ii_s)
세 가족을 두었다. roster84 대조 null band는 -0.028 ~ +0.041(최댓값 ii_s1),
roster59는 -0.037 ~ +0.090(최댓값 ii_s2). 가족별 분해에서 ii_c·ii_m·ii_s 전부를
상회했으며, 특히 **ii_m 배제는 "같은 크기의 비안구 성분으로 설명된다"는 대안
가설을 기각**한다.

### 2.3 4단계의 한계

ii_s(위상 무작위 대리)는 토포그래피·채널별 PSD·분산을 완전히 보존하며, 306개
특징 중 116개(CLR 76 + 코히런스 40)를 처치와 공유한다. ii_eye1이 이를 넘지 못한
것은 안구 정보의 부재가 아니라 그 정보가 시간영역에 있지 않다는 뜻이다. 즉 본
연구의 시간영역·주파수영역 특징으로는 안구 IC의 기여를 주 대비 기준으로
확정하기에 부족하며, 이는 특징 설계의 한계다.


## 3. 2단계 — 에폭 6초 50% 중첩

비중첩 에폭을 이어붙여 연속 신호를 복원한 뒤 stride 384로 재절단했다
(ICA 재실행 없음, 에폭 2761 -> 5401). 복원 경로는 검증되었다: `ov00`(중첩 없이
재조립) AUC가 6개 표본 전부에서 4단계 `i_brain`과 소수 셋째 자리까지 일치.

중첩(ov50) 대 비중첩(ov00) 대비:

| 표본 | dAUC | 95% CI |
|---|---|---|
| full121 | -0.005 | [-0.014, +0.004] |
| tier1_91 | -0.004 | [-0.022, +0.012] |
| tier2_86 | -0.002 | [-0.016, +0.012] |

세 tier 표본 전부에서 CI가 0을 포함했고, **CI 폭이 0.018~0.041로 이 연구에서
가장 좁았다.** 이는 정밀도 부족에 의한 귀무가 아니라 **+0.02를 초과하는 효과를
정밀하게 배제**하는 결과다. 승격 임계 +0.03에 세 표본 모두 미달하여 **부결**,
주 설계는 6초 비중첩을 유지한다.

한계(PREREG 8.3(5)): 2초 에폭은 코히런스 nperseg=256에서 창이 1개가 되어 검정
불가하고, 특징 정의 변경은 금지(PREREG 3.6)되어 있다. 따라서 2단계는 6초 격자
내에서 중첩의 무효를 보인 것이며, **에폭 설계 전체를 격차의 원인에서 배제하지는
못한다.**


## 4. 3단계 — 상한 측정

"306특징이 담은 정보를 5개 모델로 최대한 짜냈을 때의 도달점"을 측정했다
(PREREG 8.3.4). 안구 IC의 추가 정보 여부(4단계 주 대비)는 이미 종결되었으므로,
3단계는 대조군을 제외하고 절대 성능만 잰다(결정 A). 별(star) 설계로 모델 축과
표본 축을 분리했다: 중심점은 4단계 주 설정(Logistic, C=1.0, k=30, K=5)이며,
모델 축은 K=5 고정 후 5모델·하이퍼파라미터를 nested CV(내부 K=3, 사람 단위 AUC로
선택)로 스윕하고, 표본 축은 Logistic 고정 후 외부 K를 5/10/LOSO로 스윕한다.

자기검증(PREREG 8.3.4(8)) 통과: 순서 보존 mRMR의 k=30 prefix가 4단계 support와
원소 단위 일치, balanced sample_weight가 class_weight='balanced'와 계수차
0.000e+00, 중심점 10시드 AUC 0.786이 4단계 i_brain(같은 10시드 0.785)을 재현.

### 4.1 결론 1 — 상한 약 0.81, 전처리 조건 간 구분 불가

신경 계열 네 조건(i_brain, iii_noica, iv_noasr, v_wideband)의 모델축 AUC(10시드
중앙값)는 큰 표본에서 전부 0.78-0.81 고원이며 [min,max]가 서로 포개져 조건 간
구분이 불가능하다. 대표값:

| 조건 | tier2_86 | tier1_91 | full121 | tact120 |
|---|---|---|---|---|
| i_brain | 0.802 | 0.783 | 0.790 | 0.783 |
| iii_noica | 0.790 | **0.813** | 0.797 | 0.801 |
| iv_noasr | 0.809 | 0.809 | 0.802 | 0.790 |
| v_wideband | 0.802 | 0.806 | 0.803 | 0.796 |

**306특징을 5모델로 최대한 짜내도 사람 단위 AUC 약 0.81이 천장이다.** 4단계에서
"효과는 안구 IC 보유 여부"라던 것과 일관되게, 전처리 조건을 바꿔도 상한이 움직이지
않는다.

### 4.2 결론 2 — 표본은 병목이 아니다

표본 축(학습곡선)에서 K=5 -> LOSO로 학습표본을 늘려도 신경 조건은 AUC가 오르지
않고 오히려 약간 내려간다:

| 조건 | tier2_86 d(LOSO-K5) | full121 | tier1_91 |
|---|---|---|---|
| i_brain | -0.027 | -0.008 | -0.014 |
| iii_noica | -0.026 | -0.003 | -0.012 |
| iv_noasr | -0.025 | -0.016 | +0.001 |

**"표본만 더 있으면 오른다"가 아니다.** 15.8%p 격차의 귀속처에서 표본 크기가
제외된다. (예외: roster59에서 d가 크게 양수인 것은 59명에 K=5의 fold당 학습표본이
과소했던 것으로, LOSO에서 회복된 결과이며 결론을 뒤집지 않는다. 오히려 작은
표본에서 K 선택이 성능에 민감함을 보인다.)

### 4.3 결론 3 — 조합 arm도 상한을 넘지 못함

최선 신호 x 최선 모델 x LOSO(iii_noica x tier1_91 x SVM)의 탐색적 조합 arm은
AUC 0.810으로, 같은 셀의 K=5 상한(0.813)을 넘지 못했다. LOSO로 표본까지
최대화해도 이득이 없어 결론 2를 독립적으로 재확인한다.

이 값은 28개 셀 중 최고를 사후 선택한 것이므로 선택효과를 포함한다. PREREG
8.3.3(4)의 귀무 하 기대 최댓값(구성 20개 +0.067, 50개 +0.082, 288개 +0.108)을
빼면 실질값은 개별 셀 중앙값과 다르지 않다. 조합 arm은 사전지정 주 분석이 아니며,
주 결론은 4단계에서 종결되어 불변이다.

### 4.4 그리드 경계 진단 (PREREG 8.3.4(7))

1400회 선택의 분포:

- 모델: svm 34% / mlp 23% / rf 20% / logit 16% / xgb 7%. **5모델이 골고루
  뽑혀 상한이 특정 모델에 실려 있지 않다.** "더 강한 모델을 쓰지 않아서
  낮다"는 반론이 성립하지 않는다.
- k: k=15 48% / k=30 27% / k=60 25% / **k=all 0%**.
  - k=all이 한 번도 뽑히지 않음 -> mRMR 상한 60은 인공물이 아니며 상단 경계에
    걸리지 않는다(확정).
  - k=15가 최다(48%) -> **하단 경계 CAVEAT.** 그리드가 15에서 멈추므로 표
    4.1의 AUC는 이 그리드 하의 하한선이며, 진짜 최적은 15 미만일 수 있다.
    k∈{5,10} 확장(PREREG 8.3.4(7)의 결정 B)으로 더 오를 여지가 있으나, k=30·60도
    각 27%·25%로 비등하여 15~60이 완만한 고원이므로 급등하지는 않을 것으로 본다.
- RF depth=None 52% -> 무제한 깊이가 절반, 경미한 상단 경계 신호이나 과반을 약간
  넘는 수준으로 치명적이지 않다(기록).


## 5. 선행연구 대비와 격차의 귀속

표본·전처리를 선행연구에 맞춘 tact120 x v_wideband의 정확도는 0.717로,
EEG-TACT의 0.875와 **15.8%p 격차**다. 표본·전처리를 맞춘 값이므로 격차 전량이
에폭 설계와 모델에서 발생한다.

세 단계가 이 격차의 후보를 좁힌다:

- **모델**: 3단계에서 5모델을 fold마다 경쟁시켰고 상한이 특정 모델에 실리지
  않음 -> 모델 선택이 원인이 아님.
- **표본**: 3단계 학습곡선이 평평/음수 -> 표본 크기가 원인이 아님.
- **에폭 설계**: 2단계에서 6초 격자 내 중첩은 무효였으나, 2초 에폭은 특징 정의상
  검정 불가여서 완전히 배제되지 않음.
- **특징 표현**: 남는 가장 유력한 후보. 선행연구는 EEGNet+Transformer로 raw
  신호에서 표현을 학습하는 반면, 본 연구는 수작업 306특징(상대파워·TBR·CLR·
  코히런스 등)을 쓴다.

즉 격차의 남은 후보는 **특징 표현과 에폭 설계**이며, 모델과 표본 크기는 3단계로
배제된다. 이는 정직한 결과다: 선행연구를 따라잡지 못했으나, 그 이유가 "모델을
덜 밀어서"나 "표본이 적어서"가 아니라 **접근법 자체(수제 특징 대 end-to-end
학습)**임을 세 단계로 좁혔다.


## 6. 종합 한계

- **안구 IC 기여의 미확정**: 주 대비 ii_eye1은 CI 하한>0을 충족하지 못했다.
  2차 대비 ii_eyeall은 충족하나, 시간영역 대리(ii_s)를 넘지 못해 안구 정보가
  시간영역에 있지 않음을 시사한다. 본 특징 집합의 한계다.
- **하단 경계 미확장**: 3단계 상한 AUC는 k>=15 그리드 하의 하한선이다.
- **조합 arm 선택효과**: 28셀 최고를 사후 선택한 값으로, 할인 후 개별 셀과
  다르지 않다.
- **에폭 설계 미완전 배제**: 2초 에폭은 코히런스 특징 정의상 검정 불가.
- **기록 길이 교란 잔존**: ADHD 기록이 체계적으로 길어(중앙값 차 약 18s) 초시계
  기준선(길이 단독)이 표본별 0.634-0.677에 이른다. 상한 AUC 0.81은 이 기준선
  위에 있으나, 길이 교란이 완전히 제거되지는 않았다.


## 7. 산출물 위치

| 단계 | 코드 | 로그 |
|---|---|---|
| 4단계 | `src/conditions.py`, `src/features_cond.py`, `src/contrast_ic.py` | `results/conditions/contrast_all.txt` |
| 2단계 | `src/features_overlap.py`, `src/contrast_overlap.py` | `results/overlap/contrast_overlap.txt` |
| 3단계 | `src/ceiling.py`, `exploration/run_ceiling_parallel.py`, `src/ceiling_report.py` | `results/ceiling/{verify,ceiling_model,ceiling_sample}.txt` |

수치 원본 CSV(`results/ceiling/*.csv` 등)는 gitignore로 저장소에 포함하지 않으며,
코드 재실행으로 재생성한다.