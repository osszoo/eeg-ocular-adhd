# 사전등록 (PREREG) — EEG 안구 IC 기반 ADHD 분류

**동결 시점:** 이 문서가 커밋된 시점 (`git tag prereg-v1`)
**적용 범위:** 이 시점 **이후**에 수행되는 모든 분석 (계획 7~10단계)
**저장소:** github.com/osszoo/eeg-ocular-adhd

---

## 0. 이 문서의 목적

7단계(특징 확장)부터는 특징 조합·선택지가 폭발적으로 늘어난다. 결과를 본 뒤에
"좋아 보이는 설정"을 고르면 순수 잡음에서도 ΔAUC가 +0.067 수준 부풀려진다는 것을
사전 감사에서 확인했다. 이는 본 연구가 재려는 효과 크기(+0.04~0.13)와 맞먹는다.

따라서 확장에 착수하기 **전에** 주 분석을 확정하고, 그 시점을 git 태그로 고정한다.
이후 보고되는 모든 수치는 "사전지정된 주 분석" 또는 "탐색적"으로 명확히 구분된다.

---

## 1. 이미 관측한 것 (완전 공개)

본 사전등록은 눈감은(blind) 상태가 아니다. 아래는 동결 이전에 이미 관측된 결과이며,
모두 사전지정된 교란 점검의 산물이다. 은폐 없이 명시한다.

| 단계 | 관측 결과 |
|---|---|
| 1 | 분할 seed 미배선 확인·교정. 단일 분할 판정이 seed에 따라 △↔✓로 뒤집힘 |
| 2 | `class_weight='balanced'`로 특이도 붕괴(0.225) 완화. AUC 불변 |
| 3a | `cv` ddof=1 교정. neural84 불변(검증), 안구·combined 미세 이동 |
| 3b | 상대파워 CLR 변환. CLR합=0 검증, ocular84 불변(검증), neural AUC 소폭 상승 |
| 4 | **녹화 길이 단독 AUC 0.672(순수)/0.678(CV)** — combined 파이프라인(최대 0.645) 상회. ADHD 중앙 145.5s vs 대조 121.0s |
| 5a | 사전지정 주 대비(neural+P vs neural) **FAIL 2/3**. 안구 이점 대부분이 cv·succ_diff에서 발생 |
| 5b | 에폭 균등화(N=12, 84명 전원 유지) 주 대비 **PARTIAL 2/3**. cv/succ~에폭수 상관 r≈0.08로 미미 → 길이 누수 가설 약화. 창 선택(first vs even)이 ΔAUC 부호를 뒤집음 |

**핵심 인식:** 길이 누수는 주된 설명이 아닌 것으로 보이나, 안구 기여는 입증도 반증도
되지 않았다. 남은 장애물은 교란이 아니라 **정밀도**다. 분석 선택(seed·창·N·분류기)이
만드는 변동폭이 효과 크기와 맞먹는다.

---

## 2. 주 가설

ICA로 분리되어 통상 잡음으로 폐기되는 **안구 독립성분이 ADHD 판별 정보를 담고 있으며,
신경 특징에 추가했을 때 분류 성능을 향상시킨다.**

---

## 3. 주 분석 (사전지정 — 변경 불가)

### 3.1 주 대비 (교란 보정판)

**ΔAUC = AUC(neural + 길이 + 안구) − AUC(neural + 길이)**

4단계에서 녹화 길이 단독 AUC가 0.678로 확인되었다. 교란 통제의 정석은 표본을 잘라내는
것이 아니라 교란변수를 **양쪽 arm에 공변량으로 포함**하는 것이다. 두 arm 모두 길이를
보유하므로, 안구가 얻는 이득은 정의상 길이로 설명될 수 없다. 에폭 균등화와 달리 새로운
연구자 자유도(N·창 선택)를 만들지 않는다.

`길이` = 피험자별 원본 녹화 시간(초), 해당 피험자의 모든 에폭 행에 동일 값.

| 항목 | 사전지정 값 |
|---|---|
| **표본** | 84명 (eye argmax IC ≥ τ=0.5 보유자) |
| **주 대비** | ΔAUC = AUC(neural+길이+안구) − AUC(neural+길이) |
| **안구 정의** | power_ratio, cv, succ_diff (3특징 전부) |
| **주 지표** | **AUC** (임계값 무관). 정확도·민감도·특이도는 보조 |
| **주 분류기** | **Logistic Regression** (단일 지정, C=1.0 고정·튜닝 없음) |
| **CV** | StratifiedGroupKFold K=5, subject-wise, Pipeline+StandardScaler |
| **집계** | 에폭별 확률 → 피험자 단위 평균 → 피험자 단위 평가 |
| **점추정** | **50 seed 반복 CV**, ΔAUC는 seed 평균 |
| **불확실성** | **피험자 단위 짝(paired) 부트스트랩 2000회** → ΔAUC 95% CI |

### 3.2 주 분류기를 Logistic으로 지정한 근거 (결과 비의존)

1. **유효 표본이 84명**이다(에폭은 1976개이나 독립 단위는 피험자). 특징 수 대비 이
   규모에서는 정규화된 선형모델이 적정 복잡도이며, RF·SVM-RBF는 추정 분산이 크다.
   소표본에서 CV 추정의 오차막대가 매우 커진다는 것은 신경영상 ML에서 확립된 문제다.
2. **3b의 CLR 변환이 겨냥한 대상이 정확히 선형모델의 공선성**이다. 전처리 설계와 주
   모델이 논리적으로 연결된다.
3. 하이퍼파라미터를 고정하여 주 분석에 nested CV를 불필요하게 만들고, 연구자 자유도를
   제거한다.

RandomForest·SVM-RBF는 **강건성 결과로 병기**한다(비선형 관계가 존재한다면 여기서
드러난다).

### 3.3 점추정과 신뢰구간의 분리 (혼동 금지)

- **반복 CV(50 seed)** 는 분할 분산을 줄여 **점추정을 안정화**하는 장치다.
  반복끼리 동일한 84명을 재사용하므로 **신뢰구간의 근거가 될 수 없다.**
- **신뢰구간은 피험자 재표집에서만** 나온다. seed 평균 OOF 예측에 대해 피험자 단위
  짝 부트스트랩 2000회를 수행한다.
- **명시적 한계:** 이 CI는 피험자 표집 변동을 반영하지만 모델 학습 변동은 반영하지
  않는다(관행적 근사). 피험자를 재표집한 뒤 그 안에서 CV를 재수행하는 **중첩 부트스트랩**을
  민감도로 병행 보고한다. 부트스트랩 시 동일 피험자의 복제본은 반드시 같은 fold에
  배정하여 누수를 차단한다.

### 3.4 에폭 균등화를 주 분석에서 배제하는 근거

균등화는 N과 창(first-N / 균등간격)을 고르는 **새로운 자유도**를 만들며, 5b에서 그
선택이 ΔAUC 부호를 뒤집음이 실측되었다(first-N +0.128 vs 균등간격 −0.093, RF).
교란을 없애려는 통제가 새 자유도를 낳으면 본말전도이므로 주 분석에서 배제한다.
누수가 약해서가 아니라 **임의 선택을 주 분석에 들이지 않기 위해서**이다.
(참고: cv·succ_diff와 에폭 수의 상관은 r≈0.08, ρ≈0.18~0.22로 약하나 0으로 단정할
수준은 아니다. succ_diff의 ρ=0.215는 n=84에서 경계적 유의에 해당한다.)
균등화판은 민감도로 보고한다.

### 3.5 판정 규칙 (결과 보기 전 확정)

주 대비(3.1)의 95% CI를 기준으로 한다.

- **CI 하한 > 0** → 길이를 보정한 뒤에도 안구가 기여. 주 주장 성립
- **CI가 0 포함** → 정직한 귀무 결과로 보고. "84명 규모로는 이 크기의 효과를 안정적으로
  검출할 수 없다"가 결론이며, 이는 실패가 아니라 정밀도의 실측이다
- **CI 상한 < 0** → 안구 추가가 오히려 해로움을 보고

**엇갈림 규칙:** 교란 보정판(3.1)과 비보정판(4의 보조 대비)의 결론이 엇갈리면, 유리한
쪽을 택하지 않고 **'불확정'으로 보고**하며 두 값을 모두 제시한다.

어느 경우에도 **결과에 따라 주 분석을 바꾸지 않는다.**

### 3.6 특징 집합 규칙 (7단계 착수 전 확정 — 사후 가감 금지)

성능 향상은 특징 빈약성 해소에서 온다. 현재 25개는 19채널을 5부위로 평균하여 공간
해상도를 버렸고 스펙트럼 파워에 국한된다. 아래 집합을 **규칙으로 사전 확정**하며,
결과를 본 뒤 특징군을 가감하지 않는다.

| 군 | 구성 | 개수 |
|---|---|---|
| 스펙트럼 | 19채널 x 4밴드 상대파워 CLR | 76 |
| Hjorth | activity, mobility, complexity | 57 |
| 복잡도 | Higuchi FD, Katz FD, 샘플 엔트로피, 스펙트럼 엔트로피 | 76 |
| 통계 | 첨도, 왜도, 표준편차 | 57 |
| 연결성 | 부위쌍(10) x 4밴드 코히런스 | 40 |

- **TBR은 모델 특징에서 제외**한다. `log(TBR) = clr_theta - clr_beta`로 CLR과 수학적으로
  완전 중복임이 3b에서 수치 검증되었다. 임상적 친숙도를 고려해 기술통계로만 보고한다.
- **차원 축소는 fold 내부 mRMR, k=30 고정**(튜닝 없음). 유효표본 84명에 대해 통상 기준
  "특징 수 <= n/3"이 28이므로 30으로 사전 확정한다. 튜닝하지 않으므로 nested CV가
  불필요하고 자유도가 발생하지 않는다. k 민감도는 탐색적으로만 보고한다.
- **fold 밖 전역 선택은 금지**한다. 순수 잡음(참값 0.500)에서 AUC 0.741~0.768을 산출함이
  확인되었다.

### 3.7 블라인딩 규칙 (7단계 개발 기간)

특징 파이프라인 개발·디버깅 중에는 **neural(+길이) arm의 성능만 열람**한다. 안구를
포함하는 arm의 결과는 특징 집합이 동결될 때까지 산출하지 않는다. 동결 시점은 `features-v1`
태그로 고정하며, 이후에만 주 대비(3.1)를 산출한다.

목적: 특징 집합 결정이 ΔAUC 관측에 영향받지 않도록 차단한다(blinded analysis).

## 4. 보조·탐색적 분석 (전부 보고, 주 주장 근거로 쓰지 않음)

- **보조 대비(비보정판)**: ΔAUC = AUC(neural+안구) − AUC(neural). 선행연구와의 비교
  가능성을 위해 병기하되, 길이 교란이 통제되지 않았음을 명시한다
- 7단계 확장 특징(mne-features: 채널 수준, 비선형/복잡도, 연결성)의 모든 조합
- 안구 특징 사다리(P / P+C / P+S / P+C+S)
- 에폭 균등화 N 스윕 및 창 선택(first / evenly-spaced)
- RandomForest, SVM-RBF 결과
- neural121 보조 baseline
- 8단계 차원 페널티 보정 대비 combined − (neural + noise)
- 9단계 피험자 단위 특징표(84행) arm

**7단계 특징 선택은 반드시 fold 내부에서 수행한다**(mRMR/NCA를 Pipeline 안에).
fold 밖 전역 선택은 순수 잡음에서 AUC 0.741~0.768을 산출함이 확인되었으므로 금지한다.

---

## 5. 반드시 병기할 기준선

- **초시계 기준선**: 녹화 길이 단독 AUC 0.672/0.678 (4단계 실측)
- 어떤 성능 주장도 이 기준선과 나란히 제시한다
- 선행연구 86.1%는 486,780회 적합의 최댓값이므로 단일 실행 비교 대상이 아님을 명시

---

## 6. 이탈(deviation) 정책

부득이한 변경이 발생하면 **변경 전에** 본 문서에 추가 커밋으로 기록하고, 사유와 시점을
남긴다. 사후 수정은 하지 않는다. 논문에는 사전지정 결과와 이탈 내역을 함께 보고한다.

---

## 7. 결과와 무관하게 성립하는 기여

주 가설의 검정 결과가 어느 쪽이든, 아래는 독립적으로 성립한다.

1. **초시계 기준선** — 녹화 길이 단독 AUC 0.68. 90%대를 보고한 선행연구 중 이 바를
   보고한 사례 없음
2. **선택효과 분해** — 선행연구 최고치가 486,780회 적합의 최댓값임을 명시
3. **두 번째 누수 시연** — fold 밖 특징선택이 순수 잡음(참값 0.500)에서 AUC 0.741~0.768을
   산출. 이는 "정직한 subject-wise" 78~88% 범위에 정확히 앉음
4. **정밀도 실측** — 84명 규모에서 분석 선택이 만드는 ΔAUC 변동폭이 효과 크기와 맞먹음을
   실증(5b)

   ---

## 8. 이탈 기록 (deviation log)

### 8.1 채널 품질 스크리닝 도입에 따른 표본 정의 변경

**시점:** 이 절이 커밋된 시점. 스크리닝 스크립트 실행 **이전**이며, 어떤 채널
품질 지표도 아직 관측하지 않았다.

**사유:** 본 파이프라인에는 불량 채널 탐지 단계가 없었다. CAR는 매 시점 19채널
평균을 빼므로 잡음 채널 1개가 나머지 18채널 전부에 1/19 강도로 상시 주입되고,
그 오염된 신호를 ASR·ICA·ICLabel이 그대로 받는다. 죽은 채널은 경로가 다르지만
(해당 채널의 채널수준 특징 16개가 무효, 부위 평균 왜곡) 결과는 같다. 어느 쪽이든
불량 채널 1개로 그 피험자의 파이프라인 전체가 오염된다. 동일 데이터셋을 사용한
선행연구(Pastrana-Cortes et al., Technologies 2026)는 육안 검사로 v56p 1명을
제외했으나 본 연구는 121명 전원을 사용해 왔으므로, 객관 지표에 의한 검증이 필요하다.

**(1) 측정 지점** — 적재 직후 Raw(밴드패스·CAR 이전)에 pyprep 0.7.1
`NoisyChannels`를 적용한다. CAR 이전은 필수다(CAR가 오염을 전 채널로 퍼뜨리고
불량 채널 자신을 평균 쪽으로 끌어당겨 탐지력을 소멸시킨다. PREP이 재참조 이전에
불량 채널을 잡는 이유가 이것이다). 밴드패스 이전인 이유는 고주파 잡음 기준이
>50Hz 성분을 보기 때문이며, 40Hz 로우패스 후에는 이 기준이 작동하지 않는다.
저주파 표류 보정은 pyprep 내부 1Hz 하이패스에 맡긴다.
`hf_noise`가 분석 대역(0.5-40Hz) 밖을 본다는 점은 한계로 명시한다 — 이 기준은
전극 접촉 품질의 대리지표로 사용한다.

**(2) 기준과 임계값** — PREP 파이프라인(Bigdely-Shamlo et al. 2015)의 공표된
기본 임계값을 수정 없이 사용한다. 본 연구가 선택한 임계값은 없다.

| 기준 | 임계값 |
|---|---|
| nan / flat | SD 또는 MAD < 1e-15 |
| correlation / dropout | 1초 창에서 최대 절대상관 < 0.4 인 창의 비율 > 1% |
| hf_noise | (>50Hz)/(<50Hz) 진폭비의 robust z > 5 |
| deviation | robust SD의 robust z > 5 |

**RANSAC은 사용하지 않는다.** 19채널에서 25% 예측 부분집합은 4~5채널이라 구면
스플라인 예측이 성립하지 않으며, 난수 시드라는 새 자유도를 만든다.

**(3) 표본 정의** — 해당 층에서 불량 채널을 **1개 이상** 가진 피험자를 그 층의
표본에서 제외한다. **보간하지 않는다**: 19채널에서 최근접 이웃이 5~7cm이고,
보간 채널은 다른 채널의 선형결합이라 rank를 떨어뜨려 `n_components` -> IC 분해
-> ICLabel 판정 -> 안구 IC 보유자 명단(84명 정의)까지 연쇄적으로 바꾼다.

| 층 | 구성 | 지위 |
|---|---|---|
| full | 제외 없음 | 병기 |
| tier1 | nan U flat U dropout U correlation U hf_noise | 병기 |
| **tier2** | tier1 U deviation (= PREP 표준 전 기준) | **주** |

**세 표본을 항상 함께 보고한다.** 주를 tier2로 두는 근거는 결과 비의존이다 —
외부 표준을 수정 없이 적용한 판이며, 가설의 신호(전두 저주파 대진폭)를 보존하는
방향의 선택이 아니다. 세 표본의 판정이 엇갈리면 3.5절 엇갈림 규칙에 따라
'불확정'으로 보고한다.

**(4) 기준을 사후에 가감하지 않는다.** 기준별 x 채널별 플래그 표를 전수 보고한다.
`deviation`이 전두 4채널(Fp1, Fp2, F7, F8)을 비전두 대비 편식하면 그 기준이
가설 변수와 교락돼 있음을 **한계로 명시**하고 tier1 결과를 함께 제시하되,
**기준을 제거하지는 않는다.**

**(5) 기존 주 분석은 수정하지 않는다.** 8+10단계의 사전지정 주 분석
(84명, dAUC = +0.017, 95% CI [-0.051, +0.083], 귀무)은 그대로 유지·보고한다.
정제 표본에서의 주 대비는 **사전선언 민감도 분석**으로 병기한다.

**(6) 초시계 기준선을 정제 표본에서 재산출한다.** 5절 기준선(길이 단독 AUC
0.672/0.678)은 84명 기준이므로 표본이 바뀌면 기준선도 다시 재야 한다. 그룹별
배제율과 정제 표본의 길이 분포를 함께 보고한다.

**적용 범위:** 이 절 이후의 모든 신규 분석(에폭 중첩, 모델 업그레이드,
IC 제거 비용 arm)은 tier2를 기본 표본으로 하고 full·tier1을 병기한다.

---

### 8.2 correlation 기준의 적용 불가 판정

**시점:** 8.1 규칙으로 스크리닝을 1회 실행한 직후. **분류 성능(AUC)은 아직 산출하지
않았다.** 본 판정의 근거는 라벨·성능과 무관하다.

**8.1 (4)에 대한 이탈임을 명시한다.** 해당 조항은 기준의 사후 가감을 금지했다.
아래는 그 조항에 대한 이탈이며, 사전등록의 구속력을 일부 소모한다는 점을 인정한다.

**관측 사실 (results/screening/channel_quality.txt, 커밋 [해시]):**

1. `correlation`이 2299 채널-피험자 중 419건(18.2%)을 불량 판정했다. 같은 데이터에서
   하드웨어 고장 기준(nan/flat/dropout)은 **0건**이었다. 전극의 18%가 고장이면서
   그중 하나도 평탄·단절되지 않을 수는 없다. 두 기준은 서로 모순된다.
2. 플래그 수가 전극 위치를 따른다. 배열 중심으로부터의 편심도와
   Spearman rho = 0.58 (p = 0.009). 최다 플래그는 바깥 고리(P8 66, P7 51, F8 45,
   T8 42, F7 41)에 몰리고 안쪽(F4 0, F3 3, P3 4, C3 4)은 거의 없다.
3. 바깥 고리의 예외는 Fp1(14)·Fp2(17)뿐이며, 이 둘은 안구 공통성분으로 서로
   강하게 상관한다. 즉 본 기준은 "다른 채널과 소스를 공유하는가"를 측정하며,
   19채널 몽타주(최근접 이웃 약 59mm)에서 이는 공간 표본밀도의 성질이다.
   PREP의 임계값(최대상관 0.4, 나쁜 창 비율 1%)은 이웃 간격 2~3cm의 고밀도
   몽타주를 전제로 설정된 값이다.

**판정:** `correlation` 기준은 본 몽타주에서 채널 품질의 타당한 측정이 아니다.
표본 정의에서 제외한다. 계산과 보고는 계속한다.

**수정된 표본 정의:**

| 층 | 구성 | 유지 | 지위 |
|---|---|---|---|
| full | 제외 없음 | 121 | 병기 |
| tier1 | nan U flat U dropout U hf_noise | 91 | 병기 |
| **tier2** | tier1 U deviation | **86** | **주** |
| prereg81 | tier2 U correlation | 10 | 무효 판정, 기록 보존용 병기 |

**변경 범위를 correlation 하나로 한정한다.** `hf_noise`에 대해서도 우려가 있으나
(분석 대역 0.5-40Hz 밖인 >50Hz를 측정, PREP 표준 순서상 선행되어야 할 라인노이즈
제거를 수행하지 않음, 중심선 편중 Fz 5/Cz 5/Pz 4) 이는 결정적 증거가 아니라 의심이므로
**기준을 제거하지 않고 논문 한계로 기록**한다. deviation 단독 판(106명)을 병기하여
독자가 판단할 수 있게 한다.

**8.1의 나머지 조항은 모두 유효하다** — 측정 지점, 임계값 비선택, RANSAC 미사용,
불량 채널 1개 이상 시 제외, 보간 금지, 세 표본 병기, 기존 주 분석 불변경,
초시계 기준선 재산출.

**8.1의 우려 항목 해소 기록:** 8.1 (4)는 `deviation`이 전두를 편식할 가능성을
점검하도록 했다. 실측 결과 전두/비전두 플래그 비율 1.36, 최다 채널은 Fz(5)이며
Fp1 1 / Fp2 2 / F7 1 / F8 0으로 안구 교락은 확인되지 않았다. 배제 방향도
대조군 쪽(대조 18.3% vs ADHD 6.6%)이다. `deviation`을 주 규칙에 유지한다.

---

### 8.3 확장 단계 순서 변경 및 4단계(IC 제거 비용 arm) 설계 확정

**시점:** ①(채널 품질 스크리닝) 종료 직후. **정제 표본에서의 성능은 아직 산출하지
않았다.** 본 절의 모든 설계는 성능 관측 이전에 고정된다.

**지위:** 본 절의 모든 arm은 PREREG 4절의 **보조·탐색적 분석**이다. 8+10단계의
사전지정 주 분석(84명, dAUC=+0.017, 95%CI[-0.051,+0.083], 귀무)은 변경하지 않는다.

#### (1) 순서 변경: 4 -> 2 -> 3

기존 합의 순서는 2(에폭 중첩) -> 3(모델) -> 4(IC arm)였다. 아래 근거로 역순으로
바꾼다. 근거는 성능 관측과 무관하다.

- 2와 3은 절대 성능만 바꾸며 주 가설(안구 정보)을 검정하지 못한다. 4만 검정한다.
- 현재 귀무 판정은 안구 정보를 요약 통계 3개로 대표한 결과다. 4는 같은 정보를
  306개 채널수준 특징으로 재므로, "3개가 너무 거칠어서 못 잡았다"는 대안 설명을
  배제하거나 확증한다.
- 3(상한 측정)은 신호가 확정된 뒤에 수행해야 의미가 있다. 열등한 신호 위에서
  튜닝한 상한은 상한이 아니다.

2와 3은 스택하지 않는다. 셋 다 동일 기준선 대비 1요인 arm으로 독립 수행한다.

#### (2) 4단계 조건 정의 (6+4조건)

**가설 가지** — 동일한 ICA 분해를 공유하며 역투영 대상만 다르다.

| 조건 | 복원 IC | 지위 |
|---|---|---|
| i | brain 판정분만 | 기준선 |
| **ii** | brain + **top-1 eye** | **주 대비** |
| ii-all | brain + 판정 통과 eye 전부 | 2차 |
| ii-c1 | brain + 대조 IC 1순위 | 주 대조 |
| ii-c2..c5 | brain + 대조 IC 2~5순위 | 경험적 귀무 띠 |

- top-1 eye 정의는 기존 `ocular_source`와 동일(argmax=eye & p>=0.5 중 eye 확률 최대).
- 대조 IC 후보 = brain 판정도 eye 판정도 통과하지 못한 IC 전부.
- 순위 = 안구 top-1 IC와의 **센서공간 기여분산 차이**가 작은 순. 결정론적이며
  난수를 쓰지 않는다. 기여분산 = ||A[:,j]||^2 * var(s_j).
- 대조를 5개로 두는 이유: 대조 1개는 "안구가 더 낫다"만 말하지만, 분포가 있으면
  "임의의 IC 하나를 더 넣었을 때 대비 얼마나 이례적인가"를 말할 수 있다.

**절제 사다리** — 한 칸에 한 요인만 변경.

| 조건 | 밴드 | ASR | ICA |
|---|---|---|---|
| i | 0.5-40 | O | brain만 |
| iii | 0.5-40 | O | **없음** |
| iv | 0.5-40 | **없음** | 없음 |
| v | **0.5-60 + 50Hz 노치** | 없음 | 없음 |

v는 Pastrana-Cortes et al.(2026)의 전처리와 동일하다. 그들의 순서(CAR->노치->밴드패스)와
본 연구의 순서는 모두 선형연산이므로 결과가 동일하다.

#### (3) 표본

조건별로 요구 조건이 다르므로 가지별로 분리한다. 모든 층을 병기한다(8.1(3)).

| 가지 | 표본 |
|---|---|
| 절제 사다리 (i, iii, iv, v) | tier2 86 · full 121 · **선행연구 정확 표본 120(121-v56p)** |
| 가설 가지 (i, ii, ii-all, ii-c*) | tier2 & 로스터 59 · full & 로스터 84 |

#### (4) 대비와 판정 규칙

| 대비 | 정의 |
|---|---|
| **주** | AUC(ii) - AUC(i) |
| 대조 | AUC(ii-c1..c5) - AUC(i) |
| 계측 | (iii)-(i) = ICA 비용 · (iv)-(iii) = ASR 비용 · (v)-(iv) = 대역 효과 |

**가설 지지 조건 (둘 다 충족):** 주 대비의 95% CI 하한 > 0, **그리고** 주 대비가
대조 5개의 최댓값을 초과. 하나만 충족하면 '불확정'으로 보고한다.

산출 방식은 기존과 동일하다: Logistic C=1.0, fold 내부 mRMR k=30,
StratifiedGroupKFold K=5, 50 seed, 피험자 단위 짝 부트스트랩 2000회, pooled OOF AUC.

**안구 요약 3특징(power_ratio, cv, succ_diff)은 이 arm에 넣지 않는다.** 여기서 안구
정보는 신호로 들어온다. 요약 특징을 함께 넣으면 두 경로가 섞여 원인 분리가 불가능해진다.

#### (5) 해석 경계 (미리 고정)

v에서 선행연구의 87.5%가 재현되지 않아도 그것은 그들 결과의 반증이 아니다. 에폭
설계(6초 비중첩 vs 2초 50% 중첩)와 모델(Logistic vs EEGNet+Transformer + fold별
Optuna 20회)이 여전히 다르다. **v가 답하는 명제는 "전처리 선택만으로 격차가
설명되는가" 하나뿐이며, 논문에 이 경계를 명시한다.**

2초 에폭은 시도하지 않는다. 128Hz에서 2초는 256샘플이고 코히런스가 nperseg=256에서
창 1개가 되어 항등적으로 1이 된다. 대응하려면 특징 정의를 바꿔야 하므로 3.6절
(특징 집합 사후 가감 금지)에 위배된다.

#### (6) 2단계(에폭 중첩) 승격 규칙 — 미리 고정

중첩은 3.3.1에서 "표본 증가는 착시"를 근거로 기각된 바 있다. 그 근거는 **유효 표본
(사람 수)에 대해서는 여전히 옳다.** 중첩은 신뢰구간을 좁히지 못한다. 다만 학습
데이터량은 별개의 축이며, 당시 판단에는 이 구분이 없었다. 따라서 탐색적 arm으로만
수행한다.

- 안구 3특징은 **짝수 인덱스 부분열**로 계산하여 기존 값과 동일하게 고정한다.
  50% 중첩 수열의 짝수 인덱스는 비중첩 수열과 정확히 일치하므로 정의가 보존된다.
  이로써 2단계가 바꾸는 것은 신경 306특징 하나뿐이다.
- 중첩률은 50% 단일 고정. 스윕하지 않는다.
- **승격 규칙:** neural-only arm(안구 미포함)에서 세 표본 전부 dAUC >= +0.03이면
  중첩을 주 설계로 승격하고 별도 이탈로 기록한다. 미만이면 탐색적 보고만 한다.
  판정을 neural-only로 하는 것은 3.7절 블라인딩과 같은 구조로, 안구 대비를 보지
  않고 결정하므로 가설 오염이 없다.

#### (7) 3단계(상한 측정) 범위 — 미리 고정

3단계는 "정직한 subject-wise 검증에서 도달 가능한 상한"을 재는 탐색적 arm이다.
아래를 포함하며, 주 분석 설정(Logistic C=1.0, k=30, K=5)은 유지한다.

- nested CV 내부 루프에서 C 튜닝, mRMR k 튜닝
- RandomForest, SVM-RBF, XGBoost, MLP를 동일 306특징에
- **외부 fold 수 스윕(K=5/10/LOSO)**. pooled OOF AUC 구조에서는 K를 올려도 테스트
  fold 축소가 손해가 되지 않고 fold당 학습 표본만 늘어난다(86명 기준 68->78->85).
  다만 K=5는 비교 대상 선행연구 두 편이 모두 사용한 값이므로 주 분석에서 유지하고,
  스윕은 상한 보고용으로만 사용한다.