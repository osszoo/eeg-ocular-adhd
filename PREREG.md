#### 8.3.4 3단계(상한 측정) 설계 확정

8.3(7)에서 범위만 고정했던 3단계를, 4·2단계 완료 후의 사실에 맞추어
설계·비용·판정을 확정한다. 주 분석(1~7절)은 불변이며, 3단계는 8.3(7)에
이미 예고된 상한 측정의 실행 명세다.

##### (1) 무엇의 상한인가 — 세 축의 분리

3단계는 "306특징이 담은 정보를 최대로 짜냈을 때의 도달점"만 측정한다.
안구 IC의 추가 정보 여부(4단계 주 대비)는 8.3.1 절차로 이미 종결되었고,
3단계는 그 판정을 갱신하지 않는다. 이 한정(결정 A)이 3단계 범위를 대조
13개(ii_c/ii_m/ii_s)에서 해방시킨다: 상한을 재는 데 대조군은 필요 없다.

측정 대상은 두 축으로 분리한다. 섞으면 선행연구 격차(v_wideband 기준
15.8%p)의 귀속이 흐려진다.
  (가) 모델 축 — 표현력의 상한. 외부 K=5 고정, 5모델·하이퍼파라미터 스윕.
  (나) 표본 축 — 표본 크기의 기울기. Logistic 고정, 외부 K=5/10/LOSO 스윕.
       pooled OOF 구조상 K를 올리면 fold당 학습표본만 증가한다
       (tier2_86 기준 외부 학습 68 -> 78 -> 85).

두 축은 별(star) 설계로 교차한다. 중심점은 4단계 주 분석 설정
(Logistic, C=1.0, mRMR k=30, K=5)이며, 한 번에 한 축만 움직인다.
완전요인(모델 x K)을 돌리지 않는 이유는 (나)에 그리드 배수를 붙이지 않기
위해서다. 이로써 "최선 모델로 K를 스윕"할 때 생기는 선택 편향이 제거되고,
모델 기여와 표본 기여가 분리된다.

##### (2) 셀 — 6조건 x 정의되는 전 표본 = 28셀

결정 A로 대조 13개가 빠지고 6조건이 남는다:
  비안구  i_brain, iii_noica, iv_noasr, v_wideband  — 전 표본에서 정의
  안구    ii_eye1, ii_eyeall                        — 로스터 표본에서만 정의

표본은 8.1의 3층 + tact120 + 로스터 2종. 비안구 4조건 x {full121,
tier1_91, tier2_86, tact120, roster84, roster59} = 24, 안구 2조건 x
{roster84, roster59} = 4, 합 28셀. (가)는 28셀 전부, (나)는 Logistic이
저비용이므로 28셀 전부에 적용한다. 셀을 임의 선별하지 않는다(전부 산출·전부
보고 원칙). 시간이 부족하면 셀이 아니라 시드를 줄인다.

##### (3) 그리드

  Logistic  C in {0.1, 1, 10}                                      3점
  SVM-RBF   C in {1, 10} x gamma in {scale, 0.01}                  4점
  RF        max_depth in {None, 8} x min_samples_leaf in {1, 5}    4점  (n_estimators=500 고정)
  XGBoost   max_depth in {3, 6} x learning_rate in {0.05, 0.2}     4점  (n_estimators=300 고정)
  MLP       hidden in {(64,), (128,64)} x alpha in {1e-4, 1e-2}    4점  (early_stopping=True)
  선택      mRMR k in {15, 30, 60, all}                            x 4

all은 선택 없이 306특징 전부. k_max=60 절단이 상한을 만드는 인공물이 되지
않도록, 자체 특징선택 능력이 있는 트리 계열이 전 특징에서 더 오를 여지를
남긴다. RF의 n_estimators=500은 4단계와 무관한 상한 측정용 값이지만, 임의
축소(500->N)의 자의성을 피하기 위해 유지한다.

##### (4) 정합성 — nested CV의 세 조건

  a. 내부 루프의 선택 기준 = 최종 평가 지표. 최종 평가는 "에폭 확률 ->
     사람별 평균 -> pooled AUC"이므로, 내부 루프도 동일하게 사람 단위로
     집계한 AUC로 하이퍼파라미터를 고른다. sklearn GridSearchCV의 scoring은
     groups를 받지 못해 에폭 단위 AUC로 고르게 되며(에폭 사전확률 0.545가
     재유입), 따라서 내부 루프는 직접 구현한다.
  b. 내부 분할도 사람 단위. 내부 CV는 StratifiedGroupKFold, 내부 K=3.
  c. mRMR k는 prefix로 처리. mRMR은 순차 선택이므로 k=60의 선택 순서 앞
     k개가 작은 k의 선택과 동일하다. (sample, seed, 외부fold, 내부fold)당
     k_max=60까지 한 번만 적합하고 순서 보존 prefix를 취한다. 4단계의
     MRMR은 support_를 정렬 저장해 순서가 소실되므로, 3단계는 선택 순서를
     보존하는 변형을 쓰되 k=30에서 정렬 결과가 4단계 support와 일치함을
     확인한다.

##### (5) 가중치 — 전 모델 통일

전 모델에 compute_sample_weight('balanced')를 적용한다. class_weight,
scale_pos_weight, sample_weight를 모델마다 섞으면 arm 간 비교가 깨진다.
class_weight='balanced'는 내부적으로 이 가중치로 구현되므로 4단계와
수치가 동일해야 하며, 사전 측정에서 로지스틱 계수 최대차 0.000e+00으로
비트 단위 등가를 확인했다(가중치비 3.0의 불균형 부분표본 기준). sklearn
1.9의 MLPClassifier는 class_weight를 받지 않으나 fit(sample_weight=)를
받으므로, 통일 가중이 MLP에도 적용되어 4단계에서 우려했던 MLP만의 비대칭이
소거된다.

##### (6) 시드

시드 10개를 등록한다. 4단계가 50시드였고 10은 그 앞 10개 부분집합이므로,
중심점 AUC를 4단계와 직접 대조할 수 있다(자체 검증, (8) 참조). 상한 측정에서
시드는 점추정의 잡음만 줄이며 판정 기준이 아니므로 10으로 충분하다. 필요 시
50으로 연장한다. 루프는 시드 -> 셀 -> 모델 순으로 중첩하여 시드 0을 전
구성에서 끝낸 뒤 다음 시드로 넘어간다. 시드는 0부터 순차 소비되고 중단
사유가 시간이므로 선택적이지 않으며, 전 구성이 항상 동일한 시드 수를 갖는다.
CSV에 append하며, 시간 예산 소진 시 중단하고 완료된 시드 수를 보고한다.

##### (7) 필수 산출물 — 그리드 경계 진단

선택된 하이퍼파라미터의 fold·시드별 빈도표를 산출한다. 선택이 그리드
경계(예: C=10, k=all)에 몰리면 그리드가 좁은 것이므로, 이 표 없이는 "상한을
쟀다"는 주장이 성립하지 않는다. 경계 집중이 확인되면 해당 축을 넓혀 재측정한다.

##### (8) 자체 검증

별 설계의 중심점(Logistic, C=1.0, k=30, K=5)은 4단계 i_brain의 추정기와
동일하므로, 각 셀의 중심점 AUC는 4단계 해당 값을 재현해야 한다. 2단계의
ov00(비중첩 재조립이 i_brain과 +-0.000 일치)·4단계의 항등 검사(역투영
경로 검증)와 동류의 검증 고리다. 불일치는 3단계 구현 오류의 신호다.

##### (9) 조합 arm — 탐색적, 할인 병기 의무

별 설계 밖에서, 최선 신호 조건 x 최선 모델 x LOSO를 탐색적 arm으로 산출한다.
이 arm은 여러 단계의 결과를 결합하므로 선택효과를 구조적으로 포함하며
(8.3.3(4)), 사전지정 주 분석의 근거로 사용하지 않는다. 8.3.3(4)의 등록대로,
보고 시 탐색한 구성의 개수와 그에 대응하는 귀무 하 기대 최댓값(1절: 구성
20개 +0.067, 50개 +0.082, 288개 +0.108)을 반드시 병기한다. 이 병기 의무는
조합 arm에 한정되며, (2)의 28셀 주 보고에는 적용하지 않는다((10) 참조).

##### (10) 해석 경계와 한계

  - 3단계 주 보고(28셀 구성별 실제 AUC)에는 "최댓값 + 귀무 하 기대 최댓값
    (선택효과 할인)" 프레임을 지금 도입하지 않는다. 5모델 중 최선을 사후에
    읽는 것이 선택 편향임에도, 이 단계에서는 전 구성을 빠짐없이 대칭 보고하고
    최댓값을 헤드라인으로 올리지 않는 보고 규율로 대신한다. 할인 프레임은
    (7)의 진단표 위에 사후 별도 추가한다. 단, 조합 arm은 예외로서 8.3.3(4)의
    등록에 따라 산출 시점부터 할인 값을 병기한다((9)).
  - SVM-RBF는 probability=True로 fold 내부 Platt 5-fold 보정을 포함한다.
    사전 측정에서 이 비용은 미미했다(블록 2.0s).
  - 비용은 iii_noica(pool 306 전부)로 측정했으므로 상한이다. 안구 조건은
    표본이 작아 더 싸다. 블록 합 약 30s(logit 0.1/svm 2.0/mlp 2.8/xgb
    7.7/rf 17.3), 셀당 시드당 단일스레드 약 7.6분, 28셀 x 시드 1개 약
    3.6시간. 병렬 실행으로 벽시계 시간을 줄이나, 병렬화 방식은 결과를 바꾸지
    않는 구현 세부이며 등록 대상이 아니다.